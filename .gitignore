# Backups automáticos feitos pelo próprio plugin a cada escrita — são
# artefato local de segurança, não fonte de verdade, não versiona.
_spade_backups/

# Caso algum dia entre alguma dependência de verdade
node_modules/

# Lixo comum de OS/editor
.DS_Store
*.log
